# No environment variables needed for this plugin 

# Cache mode - set to true to download and serve files locally
# When false, files are served directly from usetrmnl.com
USE_CACHE=true

# Cache path - only used when USE_CACHE=true
# CACHE_PATH=/custom/cache/path

# Debug mode - uncomment to enable debug endpoint
# DEBUG_MODE=true

# Source mode - uncomment to use local files instead of downloading
# SOURCE_MODE=true

# Plugins path - uncomment to override default
# PLUGINS_PATH=/custom/plugins/path 